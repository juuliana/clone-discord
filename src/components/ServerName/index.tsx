import React from 'react';

import { Container, Title } from './styles';

const ServerName: React.FC = () => {
    return (
        <Container>
            <Title>Servidor de Hogwarts</Title>
        </Container>
    );
};

export default ServerName;